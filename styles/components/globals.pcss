* {
  &:focus {
    @apply outline-none;
  }
}

html {
  font-size: 16px;

  @media screen and (--only-phones) {
    font-size: 16px;
  }
}

body {
  @apply font-sans font-regular leading-tight text-gray-500;
}
